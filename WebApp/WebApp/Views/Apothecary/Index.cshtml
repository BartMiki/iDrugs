@using WebApp.Models.ApothecaryModels;
@model IEnumerable<ApothecaryViewModel>

@{
    ViewData["Title"] = "Aptekarze";
}

<h2>Lista wszystkich aptekarzy</h2>

<div class="btn-group">
    <button onclick="location.href='@Url.Action("Add","Apothecary")'" class="btn btn-default">
        <span class="glyphicon glyphicon-plus"></span>
        <span>Dodaj aptekarza</span>
    </button>
</div>

<br />

<table class="table table-striped">
    <thead>
        <tr class="dl-horizontal">
            <th>@Html.DisplayNameFor(model => model.FirstName)</th>
            <th>@Html.DisplayNameFor(model => model.LastName)</th>
            <th>@Html.DisplayNameFor(model => model.MonthlySalary)</th>
            <th>@Html.DisplayNameFor(model => model.IsEmployed)</th>
            <th>@Html.DisplayName("Akcje")</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var apothecary in Model)
        {
            <tr>
                <td>@Html.DisplayFor(x => apothecary.FirstName)</td>
                <td>@Html.DisplayFor(x => apothecary.LastName)</td>
                <td>@Html.DisplayFor(x => apothecary.MonthlySalary)</td>
                @{ if (apothecary.IsEmployed)
                    {
                        <td><span class="glyphicon glyphicon-ok"></span></td>
                    }
                    else
                    {
                        <td><span class="glyphicon glyphicon-remove"></span></td>
                    }
                }
                <td>
                    <div class="btn-group">
                        <button onclick="location.href='@Url.Action("Details","Apothecary", new { id = apothecary.Id })'"
                                class="btn btn-default">
                            <span class="glyphicon glyphicon-search"></span>
                            <span>Detale</span>
                        </button>
                        <button onclick="location.href='@Url.Action("Fire","Apothecary", new { id = apothecary.Id })'"
                                class="btn btn-default">
                            <span class="glyphicon glyphicon-fire"></span>
                            <span>Zwolnij</span>
                        </button>
                        <button onclick="location.href='@Url.Action("Delete","Apothecary", new { id = apothecary.Id })'"
                                class="btn btn-default">
                            <span class="glyphicon glyphicon-trash"></span>
                            <span>Usuń</span>
                        </button>
                    </div>
                </td>
            </tr>
        }
    </tbody>

</table>