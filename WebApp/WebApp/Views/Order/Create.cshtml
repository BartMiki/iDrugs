@using WebApp.Models.OrderModels
@model CreateOrderViewModel

@{
    ViewData["Title"] = "Utwórz zamównienie";
}

<h2>Utwórz zamównienie</h2>

<h4>Zamawiający</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <select asp-for="SelectedId" class="form-control selectpicker" data-live-search="true"
                    asp-items="@{new SelectList(Model.ApothecaryList,"Id","FullName")}"></select>
                <!--<label asp-for="Apothecary.LastName" class="control-label"></label>
                <input asp-for="Apothecary.LastName" class="form-control" />
                <span asp-validation-for="Apothecary.LastName" class="text-danger"></span>
                -->
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-success">
                    <span class="glyphicon glyphicon-plus"></span>
                    <span>Utwórz</span>
                </button>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index"><span class="glyphicon glyphicon-arrow-left"></span> Powrót</a>
</div>

